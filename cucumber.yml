<%
std_opts = "--format progress features --tags ~@wip -r features --strict"
wip_opts = "--color -r features --tags @wip"
%>
default: <%= std_opts %>
wip: --wip <%= wip_opts %> features
none: --format pretty
